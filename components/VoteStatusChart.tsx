import type {DashboardStats} from '../lib/types';
export default function VoteStatusChart({stats}:{stats:DashboardStats}){const total=Math.max(stats.total,1);const items=[['Will vote',stats.willVote,'bg-emerald-400'],['Not decided',stats.undecided,'bg-amber-300'],['Not voting',stats.notVote,'bg-rose-300']];return <section className="panel"><div className="mb-5"><p className="muted">Voting picture</p><h2 className="section-title">Vote status</h2></div><div className="flex h-3 overflow-hidden rounded-full bg-slate-100">{items.map(([label,value,color])=><div key={String(label)} className={String(color)} style={{width:`${Number(value)/total*100}%`}} title={`${label}: ${value}`}/>)}</div><div className="mt-5 space-y-3">{items.map(([label,value,color])=><div key={String(label)} className="flex items-center justify-between text-sm"><span className="flex items-center gap-2"><i className={`h-2.5 w-2.5 rounded-full ${color}`}/>{label}</span><strong>{Number(value).toLocaleString()}</strong></div>)}</div></section>}
