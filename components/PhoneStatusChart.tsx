import type {DashboardStats} from '../lib/types';
export default function PhoneStatusChart({stats}:{stats:DashboardStats}){const total=Math.max(stats.called+stats.needCall,1);const done=Math.round(stats.called/total*100);return <section className="panel"><div className="mb-5"><p className="muted">Call centre</p><h2 className="section-title">Phone progress</h2></div><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl bg-violet-50 p-4"><span className="muted">Called</span><strong className="mt-2 block text-3xl">{stats.called.toLocaleString()}</strong></div><div className="rounded-2xl bg-orange-50 p-4"><span className="muted">Need call</span><strong className="mt-2 block text-3xl">{stats.needCall.toLocaleString()}</strong></div></div><div className="mt-5"><div className="mb-2 flex justify-between text-sm"><span>Completion</span><b>{done}%</b></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-violet-400 transition-all duration-700" style={{width:`${done}%`}}/></div></div></section>}
