'use client';
import {useEffect,useMemo,useState} from 'react';
import {useSearchParams} from 'next/navigation';
import {getSharedAssignmentProgress,getSharedResidents,getWorkflowShare,submitSharedAssignments} from '../lib/operations';
import type {SharedAssignmentRecord,WorkflowShare} from '../lib/operations';
import type {Resident} from '../lib/types';
import {ResidentCard,ResidentFields} from '../shared/ResidentCard';

type ViewMode='all'|'selected'|'not-selected';
export default function WorkflowShareContent(){
 const token=useSearchParams().get('token')||'';
 const [rows,setRows]=useState<Resident[]>([]),[share,setShare]=useState<WorkflowShare|null>(null),[progress,setProgress]=useState<SharedAssignmentRecord[]>([]),[loading,setLoading]=useState(true),[submitting,setSubmitting]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState(''),[query,setQuery]=useState(''),[view,setView]=useState<ViewMode>('not-selected'),[selected,setSelected]=useState<Set<string>>(new Set());
 async function load(){if(!token){setError('Missing share token.');setLoading(false);return}try{const item=await getWorkflowShare(token);setShare(item);const [residentRows,assignmentRows]=await Promise.all([getSharedResidents(item),getSharedAssignmentProgress(item)]);setRows(residentRows);setProgress(assignmentRows)}catch(e){setError(e instanceof Error?e.message:'Unable to load assignment link')}finally{setLoading(false)}}
 useEffect(()=>{load()},[token]);
 const assignmentMode=share?.workflow==='assignments'&&share.can_update;
 const assigneeName=(share?.status_filter||'').trim();
 const ownProgress=useMemo(()=>progress.filter(item=>item.assignee_name.trim().toLowerCase()===assigneeName.toLowerCase()),[progress,assigneeName]);
 const savedIds=useMemo(()=>new Set(ownProgress.map(item=>String(item.resident_id))),[ownProgress]);
 const totalSelected=savedIds.size+selected.size;
 const filtered=useMemo(()=>rows.filter(r=>[r.name,r.national_id,r.phone,r.house,r.lives_in,r.election_box].join(' ').toLowerCase().includes(query.toLowerCase())),[rows,query]);
 const visible=useMemo(()=>filtered.filter(r=>{const id=String(r.id),isSelected=savedIds.has(id)||selected.has(id);if(view==='selected')return isSelected;if(view==='not-selected')return !isSelected;return true}),[filtered,view,savedIds,selected]);
 const selectableVisible=useMemo(()=>visible.filter(r=>!savedIds.has(String(r.id))),[visible,savedIds]);
 function toggle(id:string){if(savedIds.has(id))return;setSelected(current=>{const next=new Set(current);next.has(id)?next.delete(id):next.add(id);return next})}
 function toggleVisible(){setSelected(current=>{const next=new Set(current);const ids=selectableVisible.map(r=>String(r.id));const allSelected=ids.length>0&&ids.every(id=>next.has(id));ids.forEach(id=>allSelected?next.delete(id):next.add(id));return next})}
 async function submit(){if(!share)return;if(!assigneeName){setError('This link has no assignee name.');return}setSubmitting(true);setError('');setSuccess('');try{const count=await submitSharedAssignments(share,assigneeName,[...selected]);setSuccess(`${count} resident${count===1?'':'s'} saved for ${assigneeName}.`);setSelected(new Set());setView('selected');await load()}catch(e){setError(e instanceof Error?e.message:'Unable to save assignments')}finally{setSubmitting(false)}}
 if(loading)return <main className="mx-auto max-w-6xl p-5"><div className="h-32 animate-pulse rounded-2xl bg-primary-light"/></main>;
 return <main className="mx-auto max-w-6xl space-y-4 p-4 sm:p-6"><header className="panel"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Assignee</p><h1 className="mt-1">{assigneeName||'Assignment'}</h1><p className="mt-1 text-sm text-body">{totalSelected} of {rows.length} selected</p></div>{selected.size>0&&<button className="btn-primary" disabled={submitting} onClick={submit}>{submitting?'Saving…':`Save ${selected.size}`}</button>}</div></header>{error&&<div className="error-banner">{error}</div>}{success&&<div className="info-banner"><b className="text-navy">{success}</b></div>}{!error&&assignmentMode&&<section className="panel sticky top-0 z-10"><div className="grid gap-3 lg:grid-cols-[1fr_auto]"><input className="field" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search name, ID, mobile, address or box"/><button className="btn-secondary" disabled={!selectableVisible.length} onClick={toggleVisible}>{selectableVisible.length&&selectableVisible.every(r=>selected.has(String(r.id)))?'Unselect visible':'Select visible'}</button></div><div className="mt-3 grid grid-cols-3 gap-2"><FilterButton active={view==='all'} onClick={()=>setView('all')} label="All" count={rows.length}/><FilterButton active={view==='selected'} onClick={()=>setView('selected')} label="Selected" count={totalSelected}/><FilterButton active={view==='not-selected'} onClick={()=>setView('not-selected')} label="Not selected" count={Math.max(0,rows.length-totalSelected)}/></div></section>}{!error&&<section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{visible.map(r=>{const id=String(r.id),saved=savedIds.has(id),checked=selected.has(id);return <ResidentCard key={id} resident={r} selected={checked} onClick={()=>assignmentMode&&!saved&&toggle(id)} actions={saved?<span className="status-chip bg-emerald-50 text-emerald-700">Selected</span>:<label className="flex items-center gap-2 text-sm font-semibold text-body"><input type="checkbox" checked={checked} onChange={()=>toggle(id)} onClick={e=>e.stopPropagation()} className="h-5 w-5"/> {checked?'Ready to save':'Select resident'}</label>}><ResidentFields resident={r}/></ResidentCard>})}</section>}{!error&&!visible.length&&<div className="empty-state">No residents in this group.</div>}</main>
}
function FilterButton({active,onClick,label,count}:{active:boolean;onClick:()=>void;label:string;count:number}){return <button type="button" onClick={onClick} className={`rounded-xl border px-3 py-3 text-left transition ${active?'border-primary bg-primary-light text-primary':'border-border bg-card text-body hover:border-primary/40'}`}><span className="block text-xs font-semibold">{label}</span><strong className="mt-1 block text-lg text-navy">{count}</strong></button>}
